# راهنمای صفر تا صد - بعد از حذف پروژه‌ها

نگران نباش، ۵ دقیقه‌ای درستش می‌کنیم.

### مرحله ۱: یک Google Sheet بساز
1. برو به https://sheets.google.com
2. بزن **Blank spreadsheet** (شیت خالی)
3. بالاش اسمش رو بذار `Nourotour Leads`

### مرحله ۲: اسکریپت رو بساز (مهم)
1. داخل همین شیت، از منوی بالا بزن: **Extensions > Apps Script**
   - اگر فارسیه: **افزونه‌ها > Apps Script**
2. یه تب جدید باز میشه. اونجا یه فایل به اسم `Code.gs` هست که یه کد پیش‌فرض داره
3. **همه کد پیش‌فرض رو پاک کن**
4. برو داخل پروژه Nourotour تو گیت‌هاب، پوشه `google-apps-script` فایل `Code.gs` رو باز کن، کلش رو کپی کن و اینجا Paste کن
   - یا همین کد پایین رو کپی کن:

```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  if (sheet.getLastRow() === 0) {
    sheet.appendRow(["تاریخ","نوع درخواست","نام","موبایل","تور","مقصد","تعداد","تاریخ سفر","بودجه","پیام","لینک صفحه"]);
  }
  var data = JSON.parse(e.postData.contents);
  sheet.appendRow([new Date(), data.kind||"", data.fullName||"", data.phone||"", data.tour||"", data.destination||"", data.travelers||"", data.preferredDate||"", data.budget||"", data.message||"", data.pageUrl||""]);
  return ContentService.createTextOutput(JSON.stringify({result:"success"})).setMimeType(ContentService.MimeType.JSON);
}
function doGet(e) {
  return ContentService.createTextOutput(JSON.stringify({status:"ok"})).setMimeType(ContentService.MimeType.JSON);
}
```

5. بزن **Ctrl + S** تا Save بشه

### مرحله ۳: Deploy کن (مهم‌ترین بخش)
1. همونجا بالا سمت راست، دکمه آبی **Deploy > New deployment** رو بزن
2. بغل چرخ‌دنده، بزن **Select type > Web app**
3. اینا رو پر کن:
   - **Description:** `Nourotour v1`
   - **Execute as:** `Me` (خودت)
   - **Who has access:** حتما بذار **`Anyone`** - اگه اینو نذاری سایت نمی‌تونه وصل بشه
4. بزن **Deploy**
5. گوگل ازت اجازه می‌خواد: بزن **Authorize access > حسابتو انتخاب کن > Advanced > Go to Untitled project > Allow**
6. حالا یه لینک بهت میده مثل:
   `https://script.google.com/macros/s/AKfycb.../exec`
   **این لینک رو کپی کن**

### مرحله ۴: لینک رو به سایت بده
1. برو به سایتت: https://nourotour.ir
2. روی کیبورد بزن: **Ctrl + Shift + A** (پنل مدیریت باز میشه)
3. تب **عمومی** 
4. فیلد **نشانی دریافت درخواست‌ها (Google Apps Script)** همون لینک `/exec` که کپی کردی رو Paste کن
5. بزن **ذخیره اطلاعات**

تمام!

### مرحله ۵: تست
1. لینک `/exec` رو تو مرورگر باز کن، باید ببینی: `{"status":"ok"}`
2. برو تو سایت، یه فرم مشاوره پر کن و بفرست
3. برگرد تو همون Google Sheet، باید یه ردیف جدید اومده باشه

اگه نیومد:
- برو تو Apps Script > تب **Executions** (اجراها) ببین خطا چی میگه
- مطمئن شو Who has access = Anyone گذاشتی
- مطمئن شو لینک رو درست تو پنل مدیریت ذخیره کردی

---
سوالی بود همینجا بپرس.
